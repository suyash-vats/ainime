export type ItemType = {
    id: string
    title: string
    image: string
    genres: string[]
    episodeNumber: number
    releaseDate: string
}

export type ItemListType = {
    animeId: string
    animeName: string
    animeImage: string
    totalEpisodes: number
}